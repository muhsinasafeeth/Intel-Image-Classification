# Intel-Image-Classification
Builds a CNN from scratch with TensorFlow/Keras to classify 25K+ natural scene images into 6 categories. Features 3 conv blocks, dropout regularization, data augmentation, early stopping, and model checkpointing. Evaluated using accuracy, F1-score, and confusion matrix.
